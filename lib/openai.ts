import OpenAI from "openai";
const prompt = `Você escreve anúncios de leilão em português brasileiro. Responda somente JSON válido com title e description. O título deve ter menos de 90 caracteres. A descrição deve ter 60 a 140 palavras, ser clara e não inventar fatos. Não alegue autenticidade, idade, material, origem, medidas, raridade, fabricante ou modelo sem suporte explícito. Use "aparenta ser", "possivelmente" ou "conforme mostrado nas fotos" quando necessário. Não inclua preço nem emojis.`;
export async function generatePortugueseListing(input: { title: string; description: string; imageCount: number }) { const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY }); const result = await client.chat.completions.create({ model: process.env.OPENAI_MODEL || "gpt-4o-mini", response_format: { type: "json_object" }, messages: [{ role: "system", content: prompt }, { role: "user", content: JSON.stringify(input) }] }); const parsed = JSON.parse(result.choices[0]?.message.content || "{}"); if (typeof parsed.title !== "string" || typeof parsed.description !== "string") throw new Error("A geração do texto não retornou um formato válido."); return { title: parsed.title.trim(), description: parsed.description.trim() }; }
